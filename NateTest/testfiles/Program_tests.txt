###Arguments
  output application's arguments[0]
  output application's arguments's count
  loop for var arg in application's arguments:
     output arg
---
  *output_ << (((programArguments__)[0])) << "\n";
  *output_ << ((static_cast<int64_t>((programArguments__).size()))) << "\n";
  for (const auto& arg_ : programArguments__)
  {
    *output_ << (arg_) << "\n";
  }
###Name
  output application's name
---
  *output_ << ((programName__)) << "\n";