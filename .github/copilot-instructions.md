# Copilot Instructions

## Project Guidelines
- User's machine has GNU make installed; if `make` is not found in a terminal session, treat it as a PATH or shell visibility issue rather than assuming make is not installed.
- Any required confirmation prompts should be very short; avoid long commands or explanations before choices to prevent the checkbox UI from disappearing and Visual Studio from hanging.
- Avoid permission prompts unless the environment strictly requires them.